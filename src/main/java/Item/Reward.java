package Item;

import engine.GameState;

/**
 * implements a base class for Rewards in the Game.
 */
public abstract class Reward extends Item {

    public Reward(int amount) {
        super(amount);
    }
}